# Core deep learning
torch>=2.0
torchvision>=0.15

# Image/video processing
opencv-python>=4.8

# Numerics + classical ML
numpy>=1.24
scikit-learn>=1.3

# Streamlit desktop app
streamlit>=1.30

# Used in training notebook for progress bars and plots
# (remove these two lines if you only ship inference, not training)
tqdm>=4.65
matplotlib>=3.7